@model IEnumerable<ClinicManagementProject.Models.Appointments>

@{
    ViewData["Title"] = "Index";
}

<h1>Cancel Appointment</h1>


<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PatientsPatient_Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Specialization_Required)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Doctor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Appointment_time)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PatientsPatient_Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Specialization_Required)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Doctor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Appointment_time)
            </td>
            <td>
                <a asp-action="DeleteAppointment" asp-route-id="@item.Appointment_Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
